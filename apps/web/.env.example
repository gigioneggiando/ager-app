# Base URL of the AGER backend API.
# Used for public read calls (feed / articles / sources) made directly from the browser,
# and as the upstream target for the Next route-handler auth proxy (added later).
# Defaults to production if unset; override for local backend dev.
NEXT_PUBLIC_API_BASE_URL=https://api.agerculture.com
